; Local build properties.
; Copy this file to 'build.properties', uncomment the relevant settings, and replace the values with your own.

; The uri of the site.
; drupal.base_url='http://govcms.local/'

; The database settings.
; db.host=DB_HOST
; db.name=DB_NAME
; db.username=DB_USER
; db.password=DB_PASS
; db.port=DB_PORT
