diff --git a/govcms.install b/govcms.install
index 41105c2..384b256 100644
--- a/govcms.install
+++ b/govcms.install
@@ -68,6 +68,9 @@ function govcms_install() {
   // Final modules.
   module_enable(array('govcms_password_policy'));
 
+  //Enable govcms tweaks module
+  module_enable(array('govcms_tweaks'));
+
   // Clean up after install.
   govcms_cleanup();
 }
