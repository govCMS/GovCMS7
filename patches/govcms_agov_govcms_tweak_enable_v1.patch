diff --git a/agov.install b/agov.install
index 41105c2..384b256 100644
--- a/agov.install
+++ b/agov.install
@@ -68,6 +68,9 @@ function agov_install() {
   // Final modules.
   module_enable(array('agov_password_policy'));
 
+  //Enable govcms tweaks module
+  module_enable(array('govcms_tweaks'));
+
   // Clean up after install.
   agov_cleanup();
 }
